#!/bin/sh

mkdir -p /var/log/psec

chmod +x /usr/lib/psec/tests/src/tests/start.sh

rc-update add tests
rc-service tests start

# Remove unneeded big files
rm -rf /usr/lib/libQt6WebEngineCore*

exit 0